# Research context and classification browser proof

Actual TrainingAssessment, MuscleContributions, ExerciseClassification and catalog-picker components from `feature/hypertrophy-context`, mounted in a temporary Vite harness at localhost:5197/context-preview.html. The harness supplies a fixture owner; intercepted API responses simulate classification persistence and recomputed counts. This is not live authenticated frontend-to-backend proof. Temporary harness removed after verification.

Dark Chromium, desktop 1280×1000 and mobile 390×844, America/New_York. Each viewport exercises the action from Exercise matches, opens the classification panel and catalog dialog, encounters a deliberate failed save, cancels, retries successfully, changes to an unsupported catalog match, clears, closes the panel, then navigates to the current week. The selected completed week stays September 14–20 through all classification changes, while counts refresh from 0 direct/6 indirect to 3 direct/6 indirect and back. Names remain unchanged. Unsupported linked movements retain the research-review-gap message and have no classify prompt. The current week stays counts-only and forward navigation disables.

`proof.json` records requests, successful focus restoration after save, no page errors and no horizontal overflow for both viewports. The expected fixture HTTP 500 is intentional. Copy follows the short-heading, numbers-first approach of Today’s suggestion (commits 3e78078 and d141911). Screenshots show the default collapsed view, the short About these counts explanation, catalog selection, failed save and successful classification. Details are collapsed by default. Technical versions remain in API responses and docs rather than the UI.

Validation: six new API-boundary integration tests cover classify/change/clear and query-cache refresh, selected period, unsupported roles, no matches, loading, cancel, save/detail/summary failures and retry, partial evidence and source links. All 24 tests across seven files passed, including existing assessment, save-navigation, classification and invalidation regressions. TypeScript, lint, formatting, production build and whitespace checks passed. Build reports the existing large-chunk and stale Browserslist-data warnings. No Go files, backend policy or migrations changed; no Go checks were required. Live provider-authenticated browser verification remains unperformed.

## Review fix: failed detail refresh after a successful save

A successful classification followed by a failed exercise-detail refresh now focuses Retry classification. Stale classification data/actions stay hidden; updated muscle counts remain available. After retry succeeds, the saved match appears and focus returns to the panel heading. The selected week remains unchanged. This avoids losing focus when the failed refresh removes the old catalog dialog.

`detail-refresh-proof.json` and desktop/mobile detail-refresh-error screenshots cover this exact sequence in the same real components with stateful API fixtures. Both viewports report retryFocused=true, focusRestored=true (panel heading after recovery), noStaleActions=true, no page errors and no overflow. A seventh query/mutation integration test asserts saved identity, refreshed counts, focus, hidden stale actions, retry recovery and the selected period. These are simulated HTTP failures, not live-authenticated end-to-end checks.
